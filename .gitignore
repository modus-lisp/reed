# build artifacts
*.fasl
*.abcl
*.lx64fsl
.DS_Store
*~
.#*
.cache/

# libopus / FAAD2 conformance build + RFC 6716 vectors (built/fetched separately,
# live outside the repo; see Verification in the README)
opus-build/

# test corpus + decode artifacts (regenerate with test/gen-corpus.sh)
corpus/
test/out-*.wav
test/ref-*.wav

# AAC test corpus (regenerate with test/gen-aac-corpus.sh)
aac-corpus/

# Opus test artifacts (regenerate; large vectors/pcm live outside the repo)
test/*.pcm
test/*.bit
test/opus-*.pcm

# SILK conformance scratch (regenerate with opus_demo)
scratch-silk/

# Large decoded WAV artifacts (regenerate from the gen-*.sh corpus + decoders).
# A few small comparison PNGs and one short decoded WAV are kept as proof.
test/reed-music-decoded.wav
test/reed-aac-music-decoded.wav
test/reed-opus-celt-music.wav
test/reed-opus-silk-voice.wav
test/reed-opus-celt.png
